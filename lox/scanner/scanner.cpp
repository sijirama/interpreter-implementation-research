#include "scanner.h"
#include <string>
#include <vector>

using namespace std;

Scanner::Scanner(string source) : source(source) {}

vector<string> Scanner::scanTokens() {
    vector<string> tokens;
    return tokens;
}
